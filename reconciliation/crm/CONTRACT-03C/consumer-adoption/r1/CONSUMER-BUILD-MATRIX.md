# CONSUMER BUILD MATRIX (M5)

## Required matrix

The six consumers required by the r2 plan M5 boot routine:

1. `packages/contracts`
2. `packages/config`
3. `packages/integration-store`
4. `apps/integration-worker`
5. `apps/integration-api`
6. `apps/context-panel` (includes UI bundle)
7. `apps/core-1.10-media`

## Commands

For each consumer (where applicable):

```
cd <consumer-dir>
npm run build     # tsc + (context-panel) npm run build:ui
```

## Results

| Consumer | Build exit | Notes |
|---|---|---|
| `packages/contracts` | 0 | `tsc` succeeded; 711/711 contracts tests pass (separate run, see TEST-EVIDENCE.md). |
| `packages/config` | 0 | `tsc` succeeded; 25/25 tests pass. |
| `packages/integration-store` | 0 | `tsc` succeeded; 10/10 unit tests pass. |
| `apps/integration-worker` | 0 | `tsc` succeeded; 65/65 tests pass (includes pg-e2e). |
| `apps/integration-api` | 0 | `tsc` succeeded; 406/406 tests pass (includes 48 new parser/port/route/guard tests). |
| `apps/context-panel` | 0 | `tsc` succeeded; `npm run build:ui` (esbuild) succeeded; 317/319 tests pass (2 PRE_EXISTING manifest-readonly). |
| `apps/core-1.10-media` | 0 | `tsc` succeeded; 57/57 tests pass. |

**All seven build commands exit 0.** No consumer needed any
candidate-induced work-around.

## Build outputs

- `packages/contracts/dist/index.js` (frozen root) +
  `packages/contracts/dist/talent-context-read/index.js` (subpath).
- `apps/context-panel/dist/ui/bundle.js` (~1.46 MB) +
  `dist/ui/bundle.js.map` (~2.68 MB).
- All other consumers produced their standard `dist/` artifacts.

## Clean-reproducibility

- No `node_modules` was reused from a prior corrupted state.
- No `.tgz` was downloaded from a registry at install time.
- The lockfile change is limited to `packages/contracts/package-lock.json`
  and reflects the additive export map addition; consumer-side
  lockfiles were regenerated by `npm install` (Option B no-op text
  per the r2 plan) and recorded no consumer-side dep changes.
