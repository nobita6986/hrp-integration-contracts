# File Change Plan — per-slice file-level diff plan

This document records the planned file changes for each slice. No
file is modified by the planning task; this is a design record only.

## M1 — Pin/install additive candidate

### packages/integration-store/package.json
- Change `"@hrp-engagement/contracts": "file:../contracts"` to
  `"@hrp-engagement/contracts": "<pinned-spec>"`.
- Pin format options (T0 chooses):
  - workspace: `"file:../contracts"` (unchanged text; only meaningful
    if `packages/contracts` is itself replaced)
  - tarball:   `"file:../../.scratch-pkg/hrp-engagement-contracts-0.0.0-r2-candidate.0.tgz"`

### packages/integration-store/package-lock.json
- Regenerated by `npm install`. Lockfile entry for
  `node_modules/@hrp-engagement/contracts` updated.

### packages/config/package.json
- Same pattern.

### packages/config/package-lock.json
- Regenerated by `npm install`.

### apps/integration-worker/package.json
- Same pattern; dep entry changes only.

### apps/integration-worker/package-lock.json
- Regenerated by `npm install`.

### apps/integration-api/package.json
- Same pattern; dep entry changes only.

### apps/integration-api/package-lock.json
- Regenerated by `npm install`.

### apps/context-panel/package.json
- Same pattern; dep entry changes only.

### apps/context-panel/package-lock.json
- Regenerated by `npm install`.

### apps/core-1.10-media/package.json
- Same pattern; dep entry changes only.

### apps/core-1.10-media/package-lock.json
- Regenerated by `npm install`.

## M2 — Frozen-root regression test

### New files (one per consumer that imports the root)
- `packages/integration-store/tests/contract-root-regression.test.mjs`
- `apps/integration-api/tests/contract-root-regression.test.mjs`
- `apps/integration-worker/tests/contract-root-regression.test.mjs`
- `apps/context-panel/tests/contract-root-regression.test.mjs`
- `apps/core-1.10-media/tests/contract-root-regression.test.mjs`

### Existing files
- None modified.

## M3 — Adapter

### New files
- `apps/integration-api/src/orchestrator/talent-context-read-adapter.ts`
- `apps/integration-api/src/orchestrator/talent-context-read-adapter.test.mjs`

### Edited files
- None. Adapter is isolated to its own file.

## M4 — Mock wiring

### New files
- `apps/integration-api/src/orchestrator/talent-context-read-mock.ts`
- `apps/integration-api/src/orchestrator/talent-context-read-mock.test.mjs`

### Edited files
- `apps/integration-api/src/orchestrator/intake-orchestrator.ts`
  - Add optional `hrpRead` factory parameter (NOT a default).
  - Add small branch that calls adapter + mock when `hrpRead` is set.
- `apps/integration-api/tests/orchestrator.test.mjs`
  - Add one TalentContextRead happy-path test.

## M5 — Regression run

### New files
- None.

### Edited files
- Optional: `apps/context-panel/tests/panel-ui.test.mjs`
  - Add a TalentContextRead display assertion.

## M6 — Runtime interface (NOT implemented)

### New files (sketch only)
- `docs/contracts/integration-talent-context-read-runtime.md`
- `apps/integration-api/src/orchestrator/talent-context-read-runtime.ts`
  (interface + `throw new Error("not implemented")`)

### Edited files
- None.

## Files NEVER touched

- `packages/contracts/src/**` — frozen; not editable in this plan.
- `docs/contracts/handoff-core-*.md` — already-frozen history; not edited.
- `apps/integration-worker/src/**` — does not import the new subpath.
- `packages/config/src/**` — does not import the new subpath.
- `packages/integration-store/src/**` — does not import the new subpath.
- `apps/core-1.10-media/src/**` — does not import the new subpath.
- Any test that does not assert talent-context-read behaviour.
- Any file referencing internal paths
  (e.g. `packages/contracts/dist/...`, `packages/contracts/src/...`)
  of the contracts package — forbidden by brief.

## Files that may be added by future follow-up (out of scope here)

- `apps/context-panel/src/ui/components/talent-panel.tsx` — display
  redacted names from the new subpath. T0 may request this as a
  separate slice after M6.
- `packages/integration-store/src/repos/talent-context-read.ts` —
  persist TalentContextReadQueryRequest audit rows. T0 may request
  this as a separate slice after M6.