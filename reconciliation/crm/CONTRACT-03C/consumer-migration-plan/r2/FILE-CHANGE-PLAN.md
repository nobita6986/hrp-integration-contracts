# File Change Plan — per-slice file-level diff plan (r2)

This document records the planned file changes for each slice. No file
is modified by the planning task; this is a design record only.
Corrected per C-01/C-04/C-06/C-08.

## M1 — Distribution pinning (BLOCKED_BY_DISTRIBUTION_DECISION)

### Files planned under OPTION A (artifact-pinning, when landed)
- `<consumer>/package.json` (dep spec entry only — text content
  TBD by artifact-pinning task)
- `<consumer>/package-lock.json` (regenerated)
  for the six consumers listed in DEPENDENCY-INVENTORY.md.

### Files planned under OPTION B (workspace replacement, when additive source lands)
- `packages/contracts/src/**` (replacement additive source)
- `packages/contracts/package.json` (bump PACKAGE_VERSION + export map)
- `packages/contracts/package-lock.json` (regenerated)
- All six consumer `package-lock.json` files (regenerated by
  resolution; no `package.json` change on the consumer side under
  Option B).

### Files NOT listed in either option yet
- No concrete `package.json` or `package-lock.json` diff is committed
  to the r2 plan. The chosen distribution mechanism will populate the
  diff during execution.

## M2 — Root export parity

### New files
- `apps/integration-api/tests/contract-root-regression.test.mjs`
- `apps/integration-worker/tests/contract-root-regression.test.mjs`
- `apps/context-panel/tests/contract-root-regression.test.mjs`
- `apps/core-1.10-media/tests/contract-root-regression.test.mjs`
- `packages/integration-store/tests/contract-root-regression.test.mjs`
- `packages/config/tests/contract-root-regression.test.mjs`

Each test file bundles the baseline 379-name set; the test asserts
exact-set equality with the LIVE root export.

### Existing files
None modified.

## M3 — Three parsers + isolated adapter directory

### New files (per C-03)
- `apps/integration-api/src/orchestrator/talent-context-read/parser-request.ts`
- `apps/integration-api/src/orchestrator/talent-context-read/parser-result.ts`
- `apps/integration-api/src/orchestrator/talent-context-read/parser-error.ts`
- `apps/integration-api/src/orchestrator/talent-context-read/parser-request.test.mjs`
- `apps/integration-api/src/orchestrator/talent-context-read/parser-result.test.mjs`
- `apps/integration-api/src/orchestrator/talent-context-read/parser-error.test.mjs`
- `apps/integration-api/src/orchestrator/talent-context-read/index.ts`

### Edited files
None. The new directory is isolated.

### Forbidden imports
- No `packages/contracts/dist/...`
- No `packages/contracts/src/...`
- No `packages/contracts/internal/...`
- No relative imports of contracts internals (`./contracts/...`)
- No schema/type duplication

## M4 — Isolated read port + mock + route (per C-04)

### New files
- `apps/integration-api/src/orchestrator/talent-context-read/port.ts`
- `apps/integration-api/src/orchestrator/talent-context-read/port-mock.ts`
- `apps/integration-api/src/orchestrator/talent-context-read/port-mock.test.mjs`
- `apps/integration-api/src/orchestrator/talent-context-read/route.ts`
- `apps/integration-api/src/orchestrator/talent-context-read/route.test.mjs`

### Edited files
None.

### Files NEVER touched (per C-04)
- `apps/integration-api/src/orchestrator/intake-orchestrator.ts`
- `apps/integration-api/src/orchestrator/steps.ts`
- `apps/integration-api/src/orchestrator/index.ts`
- Any file that participates in the intake state machine or checkpoint
  semantics.

### Lint/grep guard (planned)
- A repo test or CI grep that asserts no source line in any of the
  forbidden files references `talent-context-read/`.

## M5 — Build/test/browser regression (per C-05)

### New files
- `apps/context-panel/tests/asset-paths.test.mjs` (normalizes scratch
  static asset paths referenced by the M5 boot routine).
- Optionally `apps/context-panel/tests/playwright.asset-paths.spec.mjs`
  for the browser-side normalization check.

### Edited files
None (product code).

## M6 — DEFERRED_SEPARATE_TASK

### New files
- None in this r2 plan. M6 owns NO production source file in r2.

### Edited files
None.

## Files that remain untouched across all slices
- `packages/contracts/src/**` (frozen at baseline; only mutable via
  separate additive migration under M1/Option B).
- `docs/contracts/handoff-core-*.md` (history).
- `apps/integration-worker/src/**` (no subpath import planned).
- `packages/config/src/**` (no subpath import planned).
- `packages/integration-store/src/**` (no subpath import planned).
- `apps/core-1.10-media/src/**` (no subpath import planned).
- `apps/context-panel/src/**` (no subpath import planned in r2).
- The intake orchestrator module (`intake-orchestrator.ts`,
  `steps.ts`, etc.) — explicitly out of scope per C-04.

## Out-of-scope future slices (not part of r2)
- Display of redacted names in the talent-panel UI.
- Persistence of TalentContextReadQueryRequest audit rows in
  integration-store.
- Real HRP runtime integration.